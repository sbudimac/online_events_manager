<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rs.raf.projekatjun.stefan_budimac_rn618">

    <application
        android:name=".application.OnlineEventsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rsrafprojekatjunstefan_budimac_rn618">
        <activity android:name=".presentaion.view.activities.EventListActivity" />
        <activity android:name=".presentaion.view.activities.NewEventActivity" />
        <activity android:name=".presentaion.view.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>